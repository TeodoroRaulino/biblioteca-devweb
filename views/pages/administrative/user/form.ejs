<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title> <%= title %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
  <link rel="stylesheet" href="/stylesheet/application.css">
</head>
<body>

  <%- include('../../../partials/sidebar'); %>

  <div class="d-flex m250 py-5 justify-content-center">
    <div class="col-5 align-self-center">
      <% if (user){%>
        <form action="/administrative/user/edit" method="post">
          <input type="hidden" name="id" value="<%= user.id %>"></input>
      <% } else { %>
        <form action="/administrative/user" method="post">
      <% } %>
        <h1 class="textCenter pb-5 text-color"><%= user ? 'Editar usuário' : 'Criar novo usuário' %></h1>
        <div class="pb-3 pt-3 col-10">
          <input name="name" type="text" class="form-control" placeholder="Nome" value="<%= user ? user.name : '' %>" required>
        </div>
        <div class="pb-3 col-10">
          <input name="cpf" type="text" class="form-control" placeholder="CPF" value="<%= user ? user.cpf : '' %>" required>
        </div>
        <div class="pb-3 col-10">
          <input name="identifier" type="text" class="form-control" placeholder="Matrícula" value="<%= user ? user.identifier : '' %>" required>
        </div>
        <div class="pb-3 col-10">
          <input name="email" type="text" class="form-control" placeholder="E-mail" value="<%= user ? user.email : '' %>" required>
        </div>
        <div class="pb-3 col-10">
          <input name="password" type="password" class="form-control" id="password" placeholder="Senha" value="<%= user ? user.password : '' %>" required>
        </div>
        <div class="pb-3 col-10">
          <input type="password" class="form-control" id="confirmation-password" placeholder="Confirmação de senha">
        </div>
        <div class="pb-3 col-10">
          <select name="type" class="form-select form-control" aria-label="Default select example">
            <option value="Aluno" selected>Aluno</option>
            <option value="Professor">Professor</option>
            <option value="Funcionário">Funcionário</option>
            <option value="Administrador">Administrador</option>
          </select>
        </div>
        <div class="col-10 textCenter">
          <input type="submit" value="<%= user  ? 'Salvar' : 'Registrar'%>" class="btn btn-success mt-3 col-4"></input>
        </div>
      </form>
    </div>
  </div>

  <script src="/javascript/application.js"></script>

</body>
</html>