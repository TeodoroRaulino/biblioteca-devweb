<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Form Livro</title>
  <link rel="stylesheet" href="/stylesheet/application.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
</head>
<body>
  
  <%- include('../../../partials/sidebar'); %>

  <div class="m250 px-5 py-5">
    <div class="row">
      <div class="row m-0">
        <div class="col-5 px-3 pb-3">
          <% if(book){ %>
            <h1 class="">Editar Livro</h1>
          <%}else{%>
            <h1 class="">Adicionar Livro</h1>
          <%}%>
        </div>
      </div>
      <%if(book){%>
        <form action="/administrative/book/edit" method="post">
          <input type="hidden" name="id" value="<%= book.id %>"></input>
      <%}else{%>
        <form action="/administrative/book" method="post">
      <%}%>
        <div class="row">
          <div class="pb-3 col-4">
            <label class="textBold">Titulo</label>
            <input name ="title" type="text" class="form-control" placeholder="Lorem Ipsun" required value="<%=book ? book.title : ''%>">
          </div>
          <div class="pb-3 col-4">
            <label class="textBold">Autor</label>
            <input name="author" type="text" class="form-control" placeholder="João e Maria" required value="<%=book ? book.author : ''%>">
          </div>
          <div class="pb-3 col-4">
            <label class="textBold">Ano de lançamento</label>
            <input name="launch_year" type="number" class="form-control" placeholder="2000" required value="<%=book ? book.launch_year : ''%>">
          </div>
        </div>
        <div class="row">
          <div class="pb-3 col-6">
            <label class="textBold">Sinopse</label>
            <textarea name="sinopse" class="form-control" placeholder="Descrição do livro..." rows="4" required value="<%=book ? book.sinopse : ''%>"></textarea>
          </div>
          <div class="pb-3 col-3">
            <label class="textBold">Edição/Volume</label>
            <input name="edition" type="number" class="form-control" placeholder="5ª Ed. / Volume 8" required value="<%=book ? book.edition : ''%>">
          </div>
          <div class="pb-3 col-3">
            <label class="textBold">Exemplares</label>
            <input name="quantity" type="number" class="form-control" placeholder="Quantidade" required value="<%=book ? book.quantity : ''%>">
          </div>
          <div class="pb-3 col-3">
            <label class="textBold">ISBN</label>
            <input name="isbn" type="form-select" class="form-control" placeholder="978-3-16-148410-0" required value="<%=book ? book.isbn : ''%>">
          </div>
          <div class="pb-3 col-3">
            <label class="textBold">Categoria</label>
            <input name="category" type="form-select" class="form-control" placeholder="Ficção científica" required value="<%=book ? book.category : ''%>">
          </div>
        </div>
        <div class="col-10 textCenter">
          <input type="submit" value="<%= book  ? 'Salvar' : 'Registrar'%>" class="btn btn-success mt-3 col-4">
          </input>
        </div>
      </form>
      </div>
    </div>
  </div>
  <script src="/javascript/application.js"></script>
</body>